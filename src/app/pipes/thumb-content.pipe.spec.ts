import { ThumbContentPipe } from './thumb-content.pipe';

describe('ThumbContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbContentPipe();
    expect(pipe).toBeTruthy();
  });
});
